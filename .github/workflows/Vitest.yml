name: Launch The Vitest Test
run-name: ${{ github.actor }} test the project code
on: [push]
jobs:
  Test-Code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone le projet P_295 
        run: git clone --branch routefinie https://github.com/Timcodingeur/Projet-JS.git
      - name: Installe les dépendances
        run: npm install --prefix ./Projet-JS/project/
      - name: Run les tests
        run: npm run --prefix ./Projet-JS/project/ test
      - name: Message de confirmation
        run: echo "Tout vas bien"
